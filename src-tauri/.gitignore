# Rust build artifacts
/target

# Bundled sidecar — generated by `pnpm run sidecar:build` (tsup), not source
/sidecar

# Bundled Node runtime — generated by `pnpm run desktop:bundle-node` (~90MB), not source
/bin

# Tauri-generated mobile/gen scaffolding
/gen

# Icon source (intermediate; the generated icons/ set is committed)
/app-icon.png
